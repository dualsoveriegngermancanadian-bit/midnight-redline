// Midnight Redline page design: a full-screen cinematic pit-board where every screen decision reinforces engine discipline and speed.

import GameCanvas from "@/components/GameCanvas";

export default function Home() {
  return <GameCanvas />;
}
